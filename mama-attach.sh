#!/usr/bin/env bash
# Подключиться к живой сессии бота «Вовка» (для мамы) и видеть всё в реальном времени.
#   Отцепиться, не закрывая бота: Ctrl-a затем d
# Если сессия где-то уже открыта — -d -r «перехватит» её сюда.
exec screen -d -r claude-mama
